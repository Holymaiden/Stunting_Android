package com.example.stunting.session

import android.content.Context

object Constants {
    // Nama file Shared Preferences
    const val PREF_NAME = ""

    // Mode privasi Shared Preferences
    const val PRIVATE_MODE = Context.MODE_PRIVATE

    // Key untuk status login
    const val IS_LOGIN = false

    // Key untuk nama
    const val KEY_USERNAME = ""

    // Key untuk id
    const val KEY_ID = 0
}